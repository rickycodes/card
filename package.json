{
  "name": "rickycodes",
  "version": "1.7.00",
  "description": "my business card",
  "bin": {
    "rickycodes": "./bin.js"
  },
  "files": [
    "bin.js",
    "dist",
    "Cargo.toml",
    "Cargo.lock",
    "src",
    "scripts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "native:build": "cargo +stable build --release",
    "wasm:build": "cargo +stable build --release --target wasm32-unknown-unknown --lib --no-default-features",
    "wasm:stage": "node ./scripts/package-wasm.js",
    "build": "npm run wasm:build && npm run wasm:stage",
    "prepack": "npm run build",
    "start:wasm": "node ./scripts/wasm-runner.js",
    "start": "node ./bin.js"
  },
  "author": "rickycodes",
  "license": "MIT"
}
